{
  "name": "fledge-plugin-discord",
  "version": "0.1.0",
  "description": "Discord webhook plugin for Fledge — receives webhooks and posts to Discord",
  "type": "module",
  "main": "src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./discord": "./src/discord.ts",
    "./verify": "./src/verify.ts",
    "./format": "./src/format.ts"
  },
  "scripts": {
    "start": "bun run src/index.ts",
    "dev": "bun --watch run src/index.ts",
    "test": "bun test",
    "lint": "bunx @biomejs/biome check src/"
  },
  "dependencies": {},
  "devDependencies": {
    "@biomejs/biome": "^1.9.0",
    "@types/bun": "^1.2.0"
  }
}
